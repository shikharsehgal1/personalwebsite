# personalwebsite

Personal site for Shikhar Sehgal — a single static `index.html` with a WebGL
fragment-shader background (domain-warped fbm noise styled after
Saffman–Taylor viscous fingering).

## Run locally

Open `index.html` directly, or serve it:

```sh
python3 -m http.server
```

No build step, no dependencies.
